from setuptools import setup, find_packages

setup(
    name="<package_name>",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A simple <package_name> package",
    python_requires=">=3.6",
)

