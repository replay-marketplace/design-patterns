from setuptools import setup, find_packages

setup(
    name="file_processing",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[],
    author="Your Name",
    description="A simple file processing package for reading, writing, and managing files and directories",
    python_requires=">=3.6",
)

