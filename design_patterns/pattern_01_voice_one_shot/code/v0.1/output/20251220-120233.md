show me how a Python function will return a type that is a list of tuples where the first item in the tuple is a string and the second one is an integer so show me the API show me the function API signature for that kind of return type
