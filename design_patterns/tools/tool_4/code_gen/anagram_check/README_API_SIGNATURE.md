# API Signature

`are_anagrams(str1: str, str2: str) -> bool` - Checks if two strings are anagrams of each other. Returns True if the strings contain the same characters with the same frequencies (case-insensitive, ignoring spaces), False otherwise.

