# API Signature

`merge_sort(arr: list) -> list` - Sorts an array using the merge sort algorithm (divide and conquer approach). Returns a new sorted list.

`merge(left: list, right: list) -> list` - Merges two sorted lists into a single sorted list. Helper function for merge_sort.

