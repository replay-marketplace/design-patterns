# API Signature

`from coder_platform.coder_agent.src.coder import coder coder(prompt: str) -> str` - Takes a prompt string and uses Claude claude-opus-4-5-20251101 to generate code, returning a JSON string containing file paths and contents.
