# API Signature

`from coder_platform.coder_platform.src.project_generator import generate_project`

`generate_project(project_goal_prompt: str) -> str` - Takes a project goal prompt, generates code using the coder agent, stores the files into a directory, and returns the directory path containing the generated project.
