# API Signature

`min_coins(coins: list[int], amount: int) -> int` - Finds the minimum number of coins needed to make the target amount using dynamic programming. Returns -1 if the amount cannot be made with the given coins.

