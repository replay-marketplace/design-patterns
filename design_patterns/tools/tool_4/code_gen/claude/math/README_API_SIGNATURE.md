# API Signature

`from src.add import add` - `add(a: int, b: int) -> int` - Add two integers

`from src.sub import sub` - `sub(a: int, b: int) -> int` - Subtract b from a

`from src.mult import mult` - `mult(a: int, b: int) -> int` - Multiply two integers

`from src.div import div` - `div(a: int, b: int) -> int` - Divide a by b (integer division)
