# API Signature

`linear_search(arr: list, target: any) -> int` - Searches for a target element in an array using linear search algorithm. Returns the index of the target if found, otherwise returns -1.

