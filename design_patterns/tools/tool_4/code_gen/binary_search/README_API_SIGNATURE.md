# API Signature

`binary_search(arr: list[int], target: int) -> int` - Searches for target element in a sorted array using binary search algorithm. Returns the index of the target if found, otherwise returns -1.

