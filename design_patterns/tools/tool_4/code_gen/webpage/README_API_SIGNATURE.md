# API Signature

`create_app() -> Flask` - Creates and configures the Flask application instance with all routes

`index() -> str` - Returns the main landing page HTML

`api_status() -> dict` - Returns JSON status of the system

`api_stats() -> dict` - Returns JSON with fake hacker stats

