# API Signature

`calculator(num1: float, num2: float, operation: str) -> float` - Perform arithmetic operation (add, sub, mult, div). Returns result or raises ValueError for invalid operation/division by zero.

